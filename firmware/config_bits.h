/* config_bits.h - Please set configuration bits in MPLAB X project or uncomment & adjust pragmas below.

Typical settings for uPIC46 with 8MHz external crystal:
- Oscillator: HS (external crystal)
- MCLR: ON (external reset)
- Watchdog Timer: OFF
- Low-Voltage Programming (LVP): OFF
- Brown-out Reset: ON (optional)

You can generate correct #pragma config lines in MPLAB X (Window -> Configuration Bits)
and paste them here. Below are example (template) pragmas you may adapt:

// #pragma config FOSC = HS        // Oscillator Selection bits (HS oscillator)
// #pragma config WDTEN = OFF      // Watchdog Timer Enable (WDT disabled)
// #pragma config LVP = OFF        // Single-Supply ICSP Enable (RB5 is digital I/O)
// #pragma config MCLRE = ON       // MCLR pin enabled; RE3 input disabled
// #pragma config PBADEN = OFF     // PORTB pins are digital on reset
*/

#ifndef CONFIG_BITS_H
#define CONFIG_BITS_H
// Keep empty or add pragmas from MPLAB X here.
#endif
